# 06-07 详细计划：实验报告终稿

## 步骤 1：整合所有章节（90 min）

把 06-03/04/05/06 的内容整合成完整终稿，确认以下结构：

```
实验报告.md
├── 1. 背景与目标（约300字，含局限声明）
├── 2. 实验设计（约400字，含环境表 + 场景矩阵 + 指标说明）
├── 3. 架构对比（约500字，配4张机制图）
├── 4. 实验结果与分析（约600字，配p1-p5图，每图一段解读）
├── 5. 成本分析（约200字，配p6图）
├── 6. 结论与选型建议（约300字，含选型表 + 局限说明）
└── 附录：实验环境版本快照、参考资料
```

**附录模板**：

```markdown
## 附录

### A. 实验环境

| 项目 | 值 |
|---|---|
| 日期 | 2026-05-26 ~ 05-31 |
| GPU | NVIDIA RTX 3060 12GB (sm_86) |
| vLLM 版本 | v___ |
| SGLang 版本 | v___ |
| 模型 | Qwen/Qwen2.5-1.5B (fp16) |
| 每组重复次数 | 3，取中位数 |

### B. 参考资料

1. vLLM 论文：Efficient Memory Management for LLM Serving with PagedAttention (arXiv:2309.06180)
2. SGLang 论文：SGLang: Efficient Execution of Structured Language Model Programs (arXiv:2312.07104)
3. EAGLE 投机解码 (arXiv:2401.15077)
4. vLLM 官方文档：https://docs.vllm.ai
5. SGLang 官方文档：https://docs.sglang.ai
6. 社区 benchmark：[06-08 交叉验证后填入]
```

## 步骤 2：三方对账（30 min）

```python
# verify_report.py — 检查报告数字与图表/CSV 是否一致
import re, csv
from pathlib import Path

# 从报告文本中提取所有数字（%、ms、tok/s）
with open("学习日志/实验报告.md") as f:
    report_text = f.read()

numbers = re.findall(r'(\d+(?:\.\d+)?)\s*(ms|tok/s|%|token)', report_text)
print(f"报告中共有 {len(numbers)} 个带单位的数字，请逐一核对：")
for n, unit in numbers[:20]:  # 显示前20个
    print(f"  {n} {unit}")
print("... 请人工核对剩余数字与对应图表/CSV")
```

人工逐项检查：正文每个数字 → 找到对应的图表或 CSV 行 → 确认一致。不一致立刻改。

## 步骤 3：逻辑链通读（30 min）

按"痛点 → 机制 → 数据 → 结论"的顺序检查全文：

- **痛点**：有 GPU 利用率数据支撑（不是空说）
- **机制**：每个机制有一张架构图（不是纯文字）
- **数据**：每个结论有对应图表（不是猜测）
- **结论**：选型建议配了场景（不是"总体来说 SGLang 更好"这种废话）

## 步骤 4：输出 PDF（可选）

如果需要 PDF 版（分享用）：

```bash
# 方法1：用 pandoc（如已安装）
pandoc 学习日志/实验报告.md -o 学习日志/实验报告.pdf \
  --pdf-engine=xelatex -V CJKmainfont="SimHei"

# 方法2：VS Code Markdown PDF 插件直接导出

# 方法3：直接用 md 文件，分享时发 GitHub 链接
```

## 今日产出（完成于 2026-06-14）
- [x] 实验报告.md 终稿整合(6章+附录，~5400字)
- [x] 8张引用图片全部存在，章节结构完整
- [x] 结构：背景/设计/架构/结果/成本/结论+附录

> 报告随06-03~06-06增量构建，终稿连贯完整。核心论点:复用率决定选型。
