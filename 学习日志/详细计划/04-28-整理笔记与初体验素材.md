# 04-28 详细计划：整理 Week 1 + "vLLM 初体验"素材

## 步骤 1：数据汇总（30 min）

新建 `学习日志/week1_数据汇总.md`，把本周散落的数据填进三张表：

```markdown
## 表1：显存与块核账（来自 04-25）
| 配置 | GPU blocks | 可缓存 token (blocks×16) | 理论最大并发 (÷4096) |
|---|---|---|---|
| max-len 4096, util 0.85 | (填) | (填) | (填) |
| max-len 4096, util 0.70 | (填) | (填) | (填) |

## 表2：APC 开关对比（来自 04-26）
| 配置 | 第1条 TTFT | 第2-10条平均 TTFT | 命中率 |
|---|---|---|---|
| APC 开 | | | |
| APC 关 | | | |

## 表3：并发基准（来自 04-27）
| concurrency | RPS | 平均 TTFT | GPU 利用率 |
|---|---|---|---|
| 1 / 5 / 10 / 20 | | | |
```

哪张表有空格，今天上午先补跑对应实验（命令在各天日志里）。

## 步骤 2：写"vLLM 初体验"（40 min）

文件 `学习日志/vLLM初体验.md`，固定结构，每节"现象→机制→我的数字"：

1. **开场**：一张 12GB 卡跑 1.5B 模型，默认配置直接 OOM（max-model-len 131072）——推理框架的第一课是显存预算
2. **PagedAttention**：表1 数据 → 解释 blocks 怎么算出来的（公式：每 token KV = 2×28层×2kv头×128×2B ≈ 28KB，自己按 Qwen2.5-1.5B 实际配置核一遍，配置看 `~/.cache/huggingface/.../config.json` 的 `num_hidden_layers`/`num_key_value_heads`/`head_dim`）
3. **Continuous Batching**：表3 → 为什么 RPS 随并发近线性涨而 GPU 利用率才慢慢饱和
4. **Prefix Caching**：表2 → TTFT 降幅 = 前缀 prefill 时间占比
5. **结尾**：一句话——vLLM 的快不是算得快，是"不浪费"（显存不浪费、算力不空等、前缀不重算）

## 步骤 3：图整理（30 min）

- 04-25 的内存流程图重画成横版（PPT 16:9 友好），存 `assets/pagedattention_flow.png`
- 用 matplotlib 把表3 画成双轴图（左轴 RPS，右轴 TTFT）：

```python
# plot_week1.py
import matplotlib.pyplot as plt
plt.rcParams['font.sans-serif'] = ['Microsoft YaHei', 'SimHei']

conc = [1, 5, 10, 20]
rps  = []   # 填表3数据
ttft = []   # 填表3数据

if __name__ == "__main__":
    fig, ax1 = plt.subplots(figsize=(8, 4.5))
    ax2 = ax1.twinx()
    ax1.plot(conc, rps, 'o-', color='tab:blue', label='RPS')
    ax2.plot(conc, ttft, 's--', color='tab:red', label='TTFT (s)')
    ax1.set_xlabel('并发数'); ax1.set_ylabel('RPS'); ax2.set_ylabel('TTFT (s)')
    plt.title('吞吐 vs 延迟：天下没有免费的并发')
    fig.savefig('assets/week1_throughput_latency.png', dpi=150, bbox_inches='tight')
```

## 步骤 4：自测（10 min）

闭卷写下三个数字并解释：你的 GPU blocks 数、APC 带来的 TTFT 降幅%、并发 1→20 的 RPS 倍数。能写出来 = Week 1 过关。

## 今日产出清单

- [ ] week1_数据汇总.md（三表填满）
- [ ] vLLM初体验.md
- [ ] 两张 PPT 素材图
