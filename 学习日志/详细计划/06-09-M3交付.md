# 06-09 详细计划：M3 阶段交付

## 步骤 1：最终数据完整性核查（40 min）

```python
# final_checklist.py — M3 交付前的完整核查
import json, csv
from pathlib import Path

REQUIRED_FILES = {
    # 实验数据
    "bench_results/vllm_fixed_output.csv":   "场景A vLLM",
    "bench_results/sglang_fixed_output.csv": "场景A SGLang",
    "bench_results/vllm_latency.csv":        "场景B vLLM",
    "bench_results/sglang_latency.csv":      "场景B SGLang",
    "bench_results/vllm_multi_turn.csv":     "场景C vLLM",
    "bench_results/sglang_multi_turn.csv":   "场景C SGLang",
    "bench_results/vllm_streaming.csv":      "场景D vLLM",
    "bench_results/sglang_streaming.csv":    "场景D SGLang",
    "bench_results/all_data.json":           "合并数据集",
    # 图表
    "assets/p1_throughput.png":              "吞吐图",
    "assets/p2_ttft_vs_conc.png":            "TTFT-并发图",
    "assets/p3_ttft_vs_input.png":           "TTFT-输入长度图",
    "assets/p4_multi_turn.png":              "多轮对话图",
    "assets/p5_gpu_utilization.png":         "GPU利用率图",
    "assets/p6_cost_comparison.png":         "成本对比图",
    "assets/pagedattention_arch.png":        "PagedAttention架构图",
    "assets/radixattention_arch.png":        "RadixAttention架构图",
    "assets/kvcache_diff.png":               "KVCache差异图",
    "assets/prefix_ratio_comparison.png":    "前缀复用率图",
    "assets/scheduler_compare.png":          "调度器对比图",
    "assets/parallelism_table.png":          "并行策略表",
    "assets/selection_guide.png":            "选型指南",
    # 报告
    "学习日志/实验报告.md":                  "实验报告终稿",
    "学习日志/选型结论.md":                  "选型结论",
    "学习日志/社区数据交叉验证-Week7.md":    "社区交叉验证",
}

if __name__ == "__main__":
    print("=== M3 交付前完整性核查 ===\n")
    missing = []
    for path, desc in REQUIRED_FILES.items():
        p = Path(path)
        status = "✓" if p.exists() else "✗ 缺少"
        size = f"({p.stat().st_size//1024}KB)" if p.exists() else ""
        print(f"  {status} {desc}: {path} {size}")
        if not p.exists(): missing.append(path)

    print(f"\n{'✓ 全部就绪，可以提交！' if not missing else f'✗ 缺少 {len(missing)} 个文件，补齐后再提交'}")
    if missing:
        for m in missing: print(f"    - {m}")
```

## 步骤 2：M3 一页纸摘要（50 min）

`学习日志/M3阶段摘要.md`，面向"不看全文的人"的精华版：

```markdown
# M3 阶段摘要：vLLM vs SGLang 对比实验

## 三句话结论

1. **通用负载下两者持平**：无共享前缀的批量推理，吞吐/延迟差异 <___\%，不构成选型依据。

2. **高复用场景 SGLang 显著更优**：多轮对话、RAG 等高前缀复用场景，
   SGLang 的 RadixAttention 将第 2 轮起的 TTFT 降低 ___\%（实测并发 8，___ms → ___ms）。
   前缀越长、复用率越高，优势越大。

3. **选框架看负载特征，不看"哪个更好"**：两框架各有主场，
   通用负载用 vLLM（生态成熟），高复用用 SGLang（缓存粒度更细）。

## 关键图表

- 核心差异：prefix_ratio_comparison.png（复用率 vs TTFT）
- 多轮证据：p4_multi_turn.png（TTFT 随轮次的变化）
- 架构差异：kvcache_diff.png（命中粒度 1 token vs 16 token）

## 实验限制

RTX 3060 12GB + Qwen2.5-1.5B，结论的定性趋势可外推，绝对数字不可直接外推到大模型。

## 下一步（Week 8）

基于以上数据制作分享 PPT，目标受众：熟悉深度学习基础的工程师。
```

## 步骤 3：Git 提交全部 M3 产物（30 min）

```bash
cd ~/vllm_learn  # 或实际路径

# 确认要提交的文件
git status

# 按类别分次提交
git add bench_results/ && git commit -m "M3: 对比实验原始数据（四场景 × 两框架）"
git add assets/p*.png assets/kvcache_diff.png assets/prefix_ratio*.png \
        assets/selection_guide.png assets/scheduler_compare.png \
        assets/pagedattention_arch.png assets/radixattention_arch.png \
        assets/parallelism_table.png assets/pp_schedule.png
git commit -m "M3: 全部分析图表（共13张）"
git add 学习日志/实验报告.md 学习日志/选型结论.md \
        学习日志/M3阶段摘要.md 学习日志/社区数据交叉验证-Week7.md
git commit -m "M3 交付：实验报告终稿 + 选型结论 + 摘要"

# 推送（需代理正常）
git push origin main
```

## 步骤 4：口头预演（10 min）

对着 M3 摘要讲 3 分钟，只用三句话结论 + 两张图，练习"被人打断只有 30 秒时如何答"：
> "同等负载下两框架持平，但多轮对话场景 SGLang 的 TTFT 低 X%，原因是缓存粒度更细——vLLM 按 16 token 整块命中，SGLang 任意长度都能命中。"

## 今日产出（完成于 2026-06-14）
- [x] final_checklist.py（25项全✓）
- [x] M3阶段摘要.md（三句话结论+30秒电梯演讲）→ [[M3阶段摘要]]
- [x] git 提交 M3 全部产物

> M3 交付完成：8数据CSV + 13图表 + 报告/结论/验证 全就绪。
