# 工作计划：04-28 ~ 06-11（重操作版）

> 每日计划遵循同一模式：**先动手看到现象 → 再解释原理 → 最后一句话自测**。
> 硬件约束：RTX 3060 12GB 单卡 / WSL2。无法实测的内容（多卡并行、FP8）已替换为等效操作。
> 通用环境变量：`NO_PROXY="*"`、模型已缓存时加 `HF_HUB_OFFLINE=1`，脚本必须有 `if __name__ == "__main__"`。

---

## Week 1 收尾

### 04-28 整理笔记 + "vLLM 初体验"素材
1. (30m) 把本周所有实验数据（blocks 核账表、APC 对比表、并发基准表）汇总到一个 `week1_数据汇总.md`
2. (40m) 用数据写"vLLM 初体验"：每个结论必须配一个自己测出的数字，禁止抄文档
3. (30m) 把 04-25 画的内存流程图和 04-27 的吞吐-延迟曲线整理成 PPT 可用版本
4. 自测：不看笔记，用 3 个自测数字讲清 vLLM 三大支柱

## Week 2：SGLang 上手

### 04-29 读 SGLang 论文（RadixAttention）
1. (20m) 先动手：跑一次 04-26 的 APC 脚本，故意构造"前缀只重叠一半块"的请求，记下命中率上不去的现象——这就是 radix 树要解决的问题
2. (60m) 读论文 arXiv:2312.07104 第 3 章，带着问题读：树怎么做到任意长度前缀命中？
3. (30m) 手画一棵 radix 树：3 个共享 system prompt 的对话插入后树长什么样，LRU 淘汰哪个叶子
4. 自测：vLLM 哈希块 vs SGLang radix 树，一句话说清粒度差异

### 04-30 安装 SGLang
1. (60m) 新建 venv 安装（别污染 vLLM 环境）：`pip install "sglang[all]" --break-system-packages`，FlashInfer 按官方 wheel 装
2. (30m) 跑通：`python3 -m sglang.launch_server --model-path Qwen/Qwen2.5-1.5B --mem-fraction-static 0.8 --context-length 4096`，curl 一次 `/v1/chat/completions`
3. (30m) 对照启动日志和 vLLM 的差异（KV pool 大小、radix cache 字样），记录踩坑点
4. 产出：安装笔记（含每个报错和解法）

### 05-01 休息/整理
1. (30m) 补 04-29/30 没完成的项
2. (20m) 闭卷复述 radix 树插入→命中→淘汰流程，卡壳处回看论文

### 05-02 体验 RadixAttention
1. (60m) 写 `06_radix_attention.py`：同一 system prompt + 多轮追问（每轮带上历史），测每轮 TTFT；SGLang 日志会直接打印 `cache hit rate`
2. (30m) 变体：前缀长度 500/1000/2000 token 各跑一组，记录命中率和 TTFT
3. (30m) 关键对照：构造"半块对齐"前缀（vLLM APC 命中不了的那种）在 SGLang 上跑，验证 radix 树能命中任意长度前缀
4. 产出：三列表格（前缀长度 | 命中率 | TTFT），第 3 步是 PPT 上最有说服力的差异点

### 05-03 体验 Structured Output
1. (50m) 用 SGLang 的 `response_format`/`json_schema` 约束生成：先简单 schema（两个字段），测 10 次有/无约束的生成速度和合法率
2. (40m) 加难度：嵌套对象 + 数组 + enum，看复杂 schema 是否拖慢、是否仍 100% 合法
3. (20m) 故意让模型"想输出"不合法内容（prompt 引导跑偏），观察 FSM 强制纠正
4. 自测：约束生成为什么几乎不增加延迟？（mask 在 logits 层，每步只是查表）

### 05-04 vLLM vs SGLang 初步跑分
1. (30m) 改造 `04_continuous_batching.py` 成框架无关版（只依赖 OpenAI 接口），存为 `07_compare_bench.py`
2. (40m) 同参数分别压 vLLM 和 SGLang：concurrency 1/5/10/20，各 40 请求，记 RPS/TTFT/ITL
3. (30m) 用 matplotlib 画两条吞吐曲线 + 两条 TTFT 曲线
4. 注意：两边 `max-model-len`/显存比例必须对齐，否则数据无效；先各跑一轮预热再计数

### 05-05 M1 总结
1. (60m) 写 M1 总结：三段式——vLLM 三大支柱（带 Week1 数据）、SGLang 两大特性（带 Week2 数据）、初步对比结论（带 05-04 曲线）
2. (30m) 列出"还不懂的问题清单"，作为 M2 源码阅读的导航
3. 自测：给同事讲 5 分钟，对方能复述出两框架核心差异算过关

## Week 3：KV Cache 源码深入

### 05-06 vLLM PagedAttention 源码
1. (20m) 先动手：`grep -rn "block_table" ~/.../vllm/v1/ | head`，定位真实文件再读（v1 引擎代码在 `vllm/v1/core/`）
2. (60m) 重点读 `kv_cache_manager.py`：`allocate_slots`、free list、块哈希；边读边在脑中对照 04-25 自己画的图
3. (40m) 验证式阅读：在源码里找到 block_size=16 的定义处、找到 APC 哈希计算函数，截图/摘录进笔记
4. 产出：源码笔记，按"我画的图 ↔ 对应代码"两栏组织

### 05-07 SGLang RadixAttention 源码
1. (20m) 定位：`sglang/srt/mem_cache/radix_cache.py`（树本体）
2. (60m) 读 `match_prefix`/`insert`/`evict` 三个函数，对照 04-29 手画的树
3. (40m) 动手验证：在本地给 `match_prefix` 加一行 print，跑 05-02 脚本，亲眼看每次请求命中多长前缀
4. 自测：radix 树节点引用计数为什么能防止"正在用的前缀被淘汰"

### 05-08 画架构对比图
1. (90m) 画两张图：PagedAttention（请求→Block Table→物理块→free list）、RadixAttention（请求→radix 树匹配→KV pool→LRU 淘汰），用 draw.io 或 mermaid
2. (30m) 第三张差异标注图：内存粒度（块 vs token）、共享方式（哈希 vs 树）、淘汰（LRU 块 vs LRU 子树）
3. 验收标准：图上每个箭头你都能指着对应源码函数名

### 05-09 HiCache + 设计哲学
1. (40m) 读 SGLang HiCache 博客（GPU→CPU→磁盘三级 KV 缓存）
2. (30m) 动手感受动机：跑 05-02 脚本但把并发拉满到显存吃紧，观察 radix 树被迫淘汰、命中率下降——HiCache 就是为了把淘汰的 KV 放到 CPU 而不是丢掉
3. (40m) 写"设计哲学"对比段：vLLM＝OS 虚拟内存思路（管好显存），SGLang＝缓存系统思路（最大化复用）
4. 自测：什么负载下 HiCache 收益最大？（长前缀+高并发+显存不够）

### 05-10 前缀复用率实验
1. (60m) 写 `08_prefix_ratio_bench.py`：构造复用率 0/25/50/75/100% 五档负载（共享前缀占总输入比例），每档 20 请求
2. (40m) vLLM 和 SGLang 各跑一遍，记 TTFT 和吞吐
3. (30m) 画"复用率 vs TTFT"双线图——预期：复用率越高 SGLang 优势越大，0% 时两者持平甚至 SGLang 略亏（树维护开销）
4. 这是全项目最重要的一张数据图，认真存原始数据

### 05-11 KV Cache 专题笔记
1. (90m) 汇总 Week 3：机制（源码笔记）+ 图（05-08）+ 数据（05-10）写成专题
2. (30m) 用 05-10 数据回答"什么时候选谁"：高前缀复用（客服/agent 多轮）→ SGLang；通用混合负载 → vLLM
3. 自测：日志里那道题——为什么块哈希链式包含前缀哈希

### 05-12 社区观点补充
1. (60m) 搜 vLLM/SGLang GitHub issues 和博客里关于两种缓存策略的讨论，摘 3-5 个有数据支撑的观点
2. (40m) 交叉验证：哪些观点和你 05-10 的数据一致/矛盾？矛盾的标注可能原因（版本、硬件、负载）
3. 产出：补充笔记，引用带链接

## Week 4：调度与并行（单卡受限周）

### 05-13 vLLM 调度器源码
1. (20m) 定位 v1 调度器：`vllm/v1/core/sched/scheduler.py`
2. (60m) 读 `schedule()` 主循环：waiting/running 队列、token 预算、抢占（preemption）
3. (40m) 动手触发抢占：把 `--max-model-len` 调大、`gpu-memory-utilization` 调低，压高并发长输出，日志里抓 `Preempted` 字样，观察被抢占请求重新排队
4. 自测：vLLM 抢占时丢掉的 KV 怎么恢复？（重算 recompute 或换出 swap）

### 05-15 Tensor Parallelism 对比（单卡：读码+模拟）
1. (60m) 读 vLLM `ColumnParallelLinear`/`RowParallelLinear`：权重怎么按列/行切、哪一步 All-Reduce
2. (40m) 单卡等效操作：用 PyTorch 手写 20 行模拟——一个 Linear 拆成两半分别算再相加，验证结果与不拆相同，亲手体会"TP 数学上无损"
3. (30m) 对比读 SGLang 对应实现，记录差异（两者都源自 Megatron 范式，差异主要在通信优化）
4. 自测：为什么 TP 切分后每层要通信一次？通信量和什么成正比？

### 05-16 EP / DP（读码+算账）
1. (50m) 读 SGLang DeepEP 文档：MoE 模型专家怎么分卡、token 怎么路由（All-to-All）
2. (40m) 动手算账：以 DeepSeek-V3（256 专家、激活 8 个）为例，手算 EP=8 时每卡专家数、单次 All-to-All 通信量
3. (30m) DP 部分：理解 SGLang 的 DP attention（KV 不切、各卡独立调度），对比 vLLM 的外部负载均衡方案
4. 自测：为什么 MoE 用 EP 比 TP 更省通信？

### 05-17 Pipeline Parallelism + Week 4 总结
1. (50m) 读 vLLM PP 实现要点：层按段切、微批次填气泡；SGLang 同理对比
2. (30m) 动手画时序图：4 段流水线 × 4 微批次的气泡图，手算气泡占比公式 (p-1)/(m+p-1)
3. (40m) 写 Week 4 总结表：TP/PP/EP/DP 四种并行的切分对象、通信原语、适用场景
4. 自测：单机多卡选 TP、跨机选 PP，为什么？

### 05-18 调度/并行对比图（PPT 素材）
1. (60m) 画调度对比图：vLLM scheduler 队列流转 vs SGLang 调度（含 radix 感知调度——优先调度能命中缓存的请求）
2. (50m) 把 05-17 的四并行总结做成 PPT 表格 + 一张"什么规模用什么并行"决策图
3. 验收：每张图配一句 takeaway

## Week 5：进阶特性

### 05-19 投机解码原理
1. (30m) 先动手感受动机：跑单请求 decode，`nvidia-smi` 看 GPU 利用率很低——投机解码就是用闲置算力"赌"未来 token
2. (50m) 学 draft-verify 范式 + 读 EAGLE 论文核心章节（特征级 drafting）
3. (30m) 读 Medusa 核心思路（多解码头 + 树状验证），手画两者结构对比图
4. 自测：为什么验证 k 个 draft token 的成本 ≈ 生成 1 个 token？（一次前向并行验证）

### 05-20 投机解码实现对比
1. (60m) 读 vLLM 投机解码入口（`--speculative-config`，支持 ngram/EAGLE/draft model）和接受率统计代码
2. (40m) 读 SGLang 对应实现（`--speculative-algorithm EAGLE`），对比集成方式差异
3. (20m) 准备明天实验：确认 1.5B 模型可用的方案——**ngram 投机最现实**（无需 draft 模型，零额外显存）
4. 自测：接受率低时投机解码为什么反而变慢？

### 05-21 投机解码实验（按单卡调整）
1. (60m) vLLM ngram 投机实验：`vllm serve Qwen/Qwen2.5-1.5B --speculative-config '{"method":"ngram","num_speculative_tokens":4,"prompt_lookup_max":4}'`，跑代码生成/文本复述类 prompt（重复性高，ngram 命中好），对比开关前后单请求延迟
2. (40m) 换自由创作类 prompt 再测——预期加速消失甚至变慢，验证"接受率决定收益"
3. (20m) EAGLE 需要训练好的 draft head，1.5B 模型没有现成的——记录此限制，引用官方 benchmark 数据替代
4. 产出：ngram 投机两类负载对比表

### 05-22 结构化输出实现原理
1. (30m) 动手复跑：vLLM 侧用 `guided_json` 跑一次 05-03 同款 schema，确认两框架都能做
2. (50m) 读 vLLM constrained decoding 集成（xgrammar/outlines 后端）
3. (40m) 读 SGLang Compressed FSM 思路：连续确定 token（如 `{"name":`）一步跳过而非逐 token
4. 自测：Compressed FSM 省的是什么时间？（确定性 token 的多次 decode 前向）

### 05-23 结构化输出深度对比
1. (50m) 走一遍正则→FSM→token mask 的构建链路：用 outlines/xgrammar 库在 notebook 里对一个小正则生成 FSM，打印状态数
2. (50m) 实验：同一复杂 schema 在 vLLM vs SGLang 各跑 20 次，对比生成速度（重点看 SGLang 压缩跳跃是否体现）
3. (20m) 整理 token-level 约束效率对比笔记
4. 自测：schema 越"死板"（确定字符多）谁优势越大？

### 05-24 权重量化（按 Ampere 调整）
1. (20m) 注意：RTX 3060 是 Ampere，**不支持 FP8**——FP8 部分只做原理笔记（per-tensor scale、Hopper TensorCore），引用官方数据
2. (60m) INT4 实测：拉一个 AWQ 模型（如 `Qwen/Qwen2.5-1.5B-Instruct-AWQ`），vLLM 加载，对比 fp16 版的显存占用、单请求延迟、生成质量（同 prompt 对比输出）
3. (30m) 笔记：AWQ（激活感知选保护通道）vs GPTQ（逐层最小化量化误差）原理一段话 + 两框架支持矩阵
4. 自测：INT4 显存省一半多，为什么延迟未必降一半？（decode 是访存瓶颈，省访存有效；但 dequant 有开销）

### 05-25 KV Cache 量化 + Week 5 总结
1. (50m) 实测 KV cache INT8/FP8_e5m2：`vllm serve ... --kv-cache-dtype fp8_e5m2`（Ampere 可用 e5m2 存储格式），对比启动日志里 GPU blocks 数——应明显变多
2. (30m) 用多出来的 blocks 换算：同显存下 max 并发提升多少，长上下文能开多长
3. (40m) Week 5 总结：投机解码/结构化输出/量化三张对比表，更新分享大纲
4. 自测：KV 量化对长上下文为什么是"质变"？

## Week 6：对比实验

### 05-26 实验方案设计
1. (40m) 调研工具：vLLM 自带 `vllm bench serve`、SGLang `sglang.bench_serving`，各跑一次默认配置感受输出格式
2. (50m) 定指标（TTFT/TPOT/Throughput/E2E）和场景矩阵：并发 {1,4,8,16,32} × 负载 {固定输出、随机输出、多轮对话}
3. (30m) 写实验方案初稿：控制变量清单（模型、max-len、显存比例、预热、重复次数 3 次取中位）
4. 单卡注意：并发 64 在 12GB 上会大量排队，上限定 32

### 05-27 实验方案定稿
1. (30m) 定模型：Qwen2.5-1.5B（fp16）为主，AWQ 版为辅助组
2. (40m) 写死所有配置进一个 `bench_config.yaml`，两框架启动命令逐参数对齐
3. (40m) 明确每个场景的 prompt 数据集（建议 ShareGPT 子集或自造固定集，保证两框架输入完全相同）
4. 产出：实验方案文档终稿，后面四天照着执行不再改

### 05-28 环境就绪 + baseline
1. (40m) 两框架环境各自验证启动→健康检查→单请求通
2. (50m) 写/改自动化脚本 `09_bench_runner.py`（asyncio 并发 + 结果落 CSV，字段：框架/场景/并发/TTFT/TPOT/E2E/吞吐）
3. (30m) 跑 baseline：并发=1 各 20 请求，确认两框架单请求延迟在合理范围且可复现
4. 产出：baseline CSV

### 05-29 吞吐量实验
1. (90m) 执行并发 1/4/8/16/32 全量跑分，两框架 × 3 次重复，自动落盘；每轮之间重启 server 保证干净状态
2. (30m) 当场画初版吞吐曲线，找拐点（吞吐不再涨、TTFT 开始飙升的并发数）
3. 注意：杀 server 后清 EngineCore 残留进程，否则下一轮显存不足

### 05-30 延迟实验
1. (50m) TTFT vs 输入长度：input 128/512/1024/2048，固定输出 64，测两框架
2. (50m) E2E vs 输出长度：固定输入 256，output 64/256/512
3. (30m) 流式场景：stream=True 测 ITL 分布（P50/P99），对比两框架的"打字流畅度"
4. 产出：延迟维度 CSV

### 05-31 特殊场景 + Week 6 总结
1. (40m) 长上下文（单卡调整）：12GB 上 1.5B 模型实测 4K/8K（开 KV 量化可冲 16K），32K 放弃实测、引用社区数据并注明
2. (40m) 多轮对话：复用 05-10 脚本，5 轮对话 × 16 并发，这是 SGLang 的主场
3. (30m) JSON mode 性能影响：同负载开关 guided_json 各测一组
4. (30m) 全部数据合并检查完整性，写 Week 6 总结

## Week 7：数据分析 + 可视化

### 06-01 吞吐/延迟图表
1. (40m) 全部 CSV 合并清洗（pandas），统一字段，剔除预热样本
2. (60m) 画四张主图：并发-吞吐、并发-TTFT、输入长度-TTFT、ITL 分布箱线图（每张双框架对比）
3. (20m) 标注异常点并写一句解释（如抢占导致的 TTFT 长尾）
4. 工具建议：matplotlib 脚本化（`10_plot.py`），别手工画，后面要反复重绘

### 06-02 资源利用率 + 选型结论
1. (50m) 补测一轮带监控：跑分同时 `nvidia-smi --query-gpu=utilization.gpu,memory.used --format=csv -l 1 > gpu.log`，画利用率随负载曲线
2. (40m) 找两框架各自的"最优负载区间"（吞吐/延迟性价比最高的并发段）
3. (30m) 写结论清单：高前缀复用/结构化输出 → SGLang 优势；通用负载/生态成熟度 → vLLM；各配一条自己的数据
4. 自测：每条结论能指出对应图表

### 06-03 实验报告·前半
1. (40m) 「背景与目标」：从选型痛点写起（一张卡怎么榨干）
2. (50m) 「实验设计」：直接引用 05-27 方案文档 + 控制变量说明
3. (50m) 「架构对比」：复用 05-08/05-18 的图，每图配 150 字解读
4. 操作要点：报告里每张图必须可由 `10_plot.py` 一键重生成

### 06-04 实验报告·后半（初稿完成）
1. (60m) 「结果与分析」：四张主图 + 资源图逐一解读，先描述现象再给机制解释
2. (40m) 「结论与选型建议」：按场景给推荐表（场景 | 推荐框架 | 依据数据）
3. (30m) 通读查数据一致性（正文数字 vs 图表 vs CSV 三方对账），列待改进项
4. 产出：报告完整初稿

### 06-05 数据归类 + 结论提炼
1. (40m) 按场景（单请求/批量/流式/多轮）重组所有数据成一张总表
2. (50m) 每场景写一条带数字的结论（如"16 并发多轮对话下 SGLang TTFT 低 X%"）
3. (30m) 形成结论文档，作为 PPT 的论点骨架
4. 自测：5 条结论里你最有信心的是哪条？数据最弱的是哪条？

### 06-06 成本对比
1. (40m) 用 06-02 的利用率数据算：达到相同 QPS 两框架各需多少"卡时"
2. (40m) 套用云价格（查当前 A10/3090 级别租价）算每百万 token 成本，列对比表
3. (30m) 画成本对比图，写结论：吞吐差 X% ≈ 成本差 X%，前缀复用场景差距放大
4. 注明：单卡 1.5B 外推到生产规模有局限，结论标注适用范围

### 06-07 报告终稿
1. (90m) 按 06-03/04 初稿 + 06-05/06 新内容整合成终稿：方法/数据/结论三章
2. (30m) 终检：逻辑链完整（痛点→机制→数据→结论）、所有图有编号和来源脚本
3. 产出：实验报告终稿 PDF/md

### 06-08 社区数据交叉验证
1. (50m) 收集两框架官方 blog 和第三方最新 benchmark（注意版本号和硬件）
2. (40m) 与自己数据对比：一致点增强信心，差异点逐条归因（硬件代差/模型规模/参数/版本）
3. (30m) 写进报告附录；分享时被问"你的数据和官方不一样"就靠这节
4. 自测：你的 3060+1.5B 结果哪些能外推、哪些不能？

### 06-09 M3 交付
1. (40m) 最后核对：数据完整性 checklist（每场景×每框架×3 次重复都在）
2. (50m) 打包 M3 交付物：报告 + 全部 CSV + 绘图脚本 + 图表目录
3. (30m) 写 M3 一页纸摘要（给不看全文的人）
4. git 提交全部产物

## Week 8：PPT

### 06-10 PPT 大纲 + 初版
1. (40m) 定结构：①为什么需要推理框架 ②vLLM 核心机制 ③SGLang 核心机制 ④对比数据 ⑤选型建议
2. (30m) 提炼 3-5 个 Takeaway（直接用 06-05 结论文档）
3. (60m) 搭 PPT 骨架：每页先只写标题和一句话论点，图占位
4. 原则：一页一论点，数据图全部来自自己的实验

### 06-11 Part 1 + vLLM 部分制作
1. (50m) Part 1「为什么需要推理框架」：用 04-27 的 GPU 利用率观察做开场（decode 时 GPU 在摸鱼）
2. (60m) vLLM 部分：三大支柱各一页（05-08 架构图 + Week1 数据），叙事链照 04-27 的因果链
3. (30m) 过一遍口头讲稿，每页 ≤60 秒
4. 自测：抽掉所有文字只看图，听众能否跟上故事线
